<UserControl x:Class="Client.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:core="clr-namespace:Client.Core;assembly=Client.Core"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <DockPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="5">
                <Button Command="{Binding NavigateCommand}" CommandParameter="{x:Static core:NavigationSources.LoginSource}" Margin="5">Log In</Button>
            </StackPanel>
            <TextBlock Text="In Progress..." FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </DockPanel>
    </Grid>
</UserControl>