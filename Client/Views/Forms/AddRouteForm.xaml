<UserControl x:Class="Client.Views.AddRouteForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding OnLoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <materialDesign:Card Padding="35" Margin="10" VerticalAlignment="Center" >
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBox materialDesign:HintAssist.Hint="Name" Style="{StaticResource ValidationTextBox}" />
                <TextBox materialDesign:HintAssist.Hint="Mark" Style="{StaticResource ValidationTextBox}" />
                <xctk:CheckComboBox ItemsSource="{Binding RailwayStations}" Delimiter="; " Margin="0,15,0,15"/>
                <Button Margin="0,20,0,0">Submit</Button>
            </StackPanel>
        </ScrollViewer>
    </materialDesign:Card>
</UserControl>