<UserControl x:Class="Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:Client.ViewModel"
             xmlns:rules="clr-namespace:Client.ValidationRules"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModel:LoginViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="ValidationTextBlock" BasedOn="{StaticResource MaterialDesignCaptionTextBlock}">
            <Setter Property="Margin" Value="0,0,0,15" />
            <Setter Property="Foreground" Value="Red" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <materialDesign:Card Padding="32" Margin="16" MaxWidth="500">
            <StackPanel>
                <TextBox x:Name="UsernameTextBox" materialDesign:HintAssist.Hint="Username"  MaxLength="20"
                         Text="{Binding LoginModel.Username, Mode=TwoWay}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <TextBlock Text="{Binding LoginModel.ValidationErrors[Username]}" Style="{StaticResource ValidationTextBlock}" />

                <PasswordBox x:Name="textBoxPassword" materialDesign:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" />
                <TextBlock Text="{Binding LoginModel.ValidationErrors[Password]}" Style="{StaticResource ValidationTextBlock}" />

                <Button x:Name="buttonLogin" Content="Login"
                        Command="{Binding PasswordCommand}"
                        CommandParameter="{Binding ElementName=textBoxPassword}" />
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>